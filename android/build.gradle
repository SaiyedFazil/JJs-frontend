buildscript {
    ext {
        buildToolsVersion = "36.0.0"
        minSdkVersion = 26
        compileSdkVersion = 36
        targetSdkVersion = 36
        ndkVersion = "27.1.12297006"
        kotlinVersion = "2.1.20"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

apply plugin: "com.facebook.react.rootproject"

// Redirect CMake .cxx build directories to a short path to avoid
// CMake's CMAKE_OBJECT_PATH_MAX limit of 250 characters on Windows.
subprojects { subproject ->
    subproject.plugins.withId("com.android.library") {
        try {
            subproject.android.externalNativeBuild.cmake.buildStagingDirectory =
                new File("C:/cxx/${subproject.name}")
        } catch (Exception ignored) {}
    }
    subproject.plugins.withId("com.android.application") {
        try {
            subproject.android.externalNativeBuild.cmake.buildStagingDirectory =
                new File("C:/cxx/${subproject.name}")
        } catch (Exception ignored) {}
    }
}
